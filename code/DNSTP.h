#ifndef DNSTP_DNSTP
#define DNSTP_DNSTP

#include <vector>

namespace DNSTP
{

// Test whether one vector has all its elements above a second vector.
bool is_above(const std::vector<double>& v1,
                    const std::vector<double>& v2);


} // namespace DNSTP

#endif

